apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - job.yaml
configMapGenerator:
  - name: configmap-job-jmeter
    files:
    - jmeter.jmx
    - jmeter.properties
    options:
      disableNameSuffixHash: true
